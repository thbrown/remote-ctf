{
  "name": "foundry-ctf",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "pretest": "pnpm --filter @foundry-ctf/shared build",
    "test": "pnpm -r test",
    "pretypecheck": "pnpm --filter @foundry-ctf/shared build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "5.4.5"
  },
  "packageManager": "pnpm@9.1.0"
}
